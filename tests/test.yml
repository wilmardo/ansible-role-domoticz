---
- hosts: all
  become: yes
  roles:
    - {role: "{{ lookup('env', 'TRAVIS_REPO_SLUG') | basename }}" } #Gets only the repository name

  post_tasks:
    - name: Wait for Domoticz to succesfully restart
      wait_for:
        port: "{{ domoticz_port }}"

    - name: Test Domoticz webinterface
      uri:
        url: "http://{{ inventory_hostname }}:{{ domoticz_port }}"

    - name: Grab system Logs
      shell: "PAGER=cat journalctl -xn || cat /var/log/messages || cat /var/log/syslog"
      changed_when: false
      ignore_errors: yes